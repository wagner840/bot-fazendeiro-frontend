name: frontend-ci-cd

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Build
        run: npm run build

  deploy:
    if: github.ref == 'refs/heads/main'
    needs: [test-and-build]
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Coolify Frontend deploy
        run: curl -fsSL -X POST "${{ secrets.COOLIFY_FRONTEND_DEPLOY_HOOK }}"

